OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=

ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://api.anthropic.com
ANTHROPIC_MODEL=

FRONTEND_ORIGIN=http://localhost:5173
SNAPSHOT_FILE=/app/data/snapshot.json

# Optional: Investing.com / InvestingPro browser session Cookie (not an official API key).
# After logging into https://www.investing.com in a browser, copy the Cookie request header
# and paste it here as a single line. Enables per-ticker instrument news when Cloudflare allows.
# Do not commit real cookies. Rotate if leaked.
# INVESTING_SESSION_COOKIE=ses_id=...; domain_user=...
